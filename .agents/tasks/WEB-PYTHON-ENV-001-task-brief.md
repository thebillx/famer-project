# Task Brief

## Task ID

WEB-PYTHON-ENV-001

## Status

IMPLEMENTED

## Title

Disposable verified Python 3.12 WEB-SEC validation profile

## Business goal

Unblock the contract-required Python tests for WEB-SEC-CONTRACT-001 with an isolated Python 3.12 environment containing only the exact 12 selected `pyproject.toml`-backed roots and their transitive wheels. The profile must not replace the user's unsupported repository `.venv`, add a YAML parser or other validation-only package, change product dependencies, compile source distributions, install unused product subsystems, or mutate repository application files.

## Current behavior

- `pyproject.toml` requires Python `>=3.12,<3.13`; the repository `.venv` is Python 3.13.1.
- The Codex host provides `/Users/bill/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3.12`, which reports arm64 Python 3.12.13.
- That host runtime is current-user owned and ad-hoc signed, has no publisher receipt or sealed-resource signature, and therefore is not claimed as upstream-provenance verified.
- Its populated base site-packages/pip 26.2.1 are outside this task's trust chain and must never be imported or invoked. Its bundled `ensurepip` wheel is pip 25.0.1.
- No Python lockfile or supported repository Python 3.12 environment exists.
- WEB-SEC phase A converts the clean tracked OpenAPI YAML model to strict deterministic JSON at the same `docs/api/openapi.yaml` path, proves decoded deep equality, and updates the contract test to semantic stdlib `json.loads` assertions before fresh provisioning. The fresh profile therefore needs no YAML parser or validation-only package.
- The first full-project provisioning attempt stopped before product installation because `rasterio==1.4.3` wheel member `rasterio/.dylibs/libsqlite3.0.dylib` contained external `LC_RPATH /private/tmp/local/lib`, which the approved Mach-O containment policy correctly rejected. The later minimized PyYAML-bearing candidate and the first fresh no-YAML candidate also transferred no consumer authority. All three temporary roots are now absent; their earlier receipt hashes are historical audit context only and are not live acceptance evidence.
- Phase A is now frozen in the repository: `docs/api/openapi.yaml` SHA-256 `e71bea8058e4436be5c2b22c695616a6d14036c4069c4b24a10cb64ce647787a` and `tests/contract/test_foundation_contract.py` SHA-256 `db271e520ccc3d1aef273cbca50fb9528306ecd3ce4ac6ead2a65e0dae4f6b63`.
- On 2026-08-21 the operating system cleared the task's `/private/tmp` roots and external evidence between goal continuations, including the later fresh candidate `/private/tmp/agriscope-py312.1grxdH`, before consumer authority transferred. Current filesystem inspection confirms all three prior roots and the temporary evidence directory are absent. Their previously recorded hashes remain historical task context but are not treated as live/reproducible acceptance inputs.
- A first durable-cache leaf, `/Users/bill/Library/Caches/AgriScope/WEB-PYTHON-ENV-001/agriscope-py312.ng9ITm`, stopped before any Python, venv, network, package, consumer-test, or repository-write action when the historical 947-record TCB pin did not match the complete current 943-record surface. It transferred no authority and must be rejected, evidenced, and removed literally before any fresh leaf is created; it is never resumed or reused.
- Read-only import analysis proves WEB-SEC's owned runtime and required unit/contract/integration tests do not import or execute Rasterio, Uvicorn, Alembic, Redis, Dramatiq, python-jose, Ruff, or mypy. Shapely is required by the existing geometry regression and itself requires NumPy, so the profile includes the repository's exact NumPy pin only as Shapely's required dependency. The new profile excludes the unrelated roots instead of weakening the native-linkage rule.

## Explicit trust decision

- For this disposable local validation task, the orchestrator accepts the pre-existing Codex-supplied Python runtime as a host trusted-computing-base assumption, not as proof of python.org provenance. Python.org publishes 3.12.13 as source-only, so this task does not pretend an official 3.12.13 macOS installer exists.
- Before use, snapshot and revalidate the exact TCB surface: `bin/python3.12`; root `lib/*.dylib`; and every regular file under `lib/python3.12` except `site-packages`. Build one record per file as the JSON array `[relative_path,"file",mode_decimal,size_decimal,sha256_hex]`, where the path is relative to the TCB root, uses `/`, is Unicode NFC, and the selected surface rejects symlinks/special files. Sort records by the UTF-8 bytes of `relative_path`; serialize the outer array with UTF-8, `ensure_ascii=false`, separators `,` and `:`, no key sorting/indentation, and one final LF; SHA-256 those exact bytes. The newly reviewed whole current surface is exactly 943 records and digest `be62e615e0f9b9f2741e977ca7dc84374a1db9c5d98fc17bdc3e770e28e277c4`: 939 stdlib files, three root dylibs, and the interpreter. It intentionally includes all 193 selected `.pyc` files because `-B` prevents bytecode writes but does not prevent bytecode loading. The interpreter SHA-256 remains `71720f1fc66989ebd691e81c96111b47ae6ff3f1a478666084d1cacbf0fccbf2`.
- The superseded historical identity was 947 records/digest `2446cbb06378a05ebf5fe2479cc6f057d7eba36e389219b432b45363b99fae28`. No historical record-level manifest survived, so the four-record difference is not attributed or characterized as harmless; the orchestrator explicitly accepts the complete current 943-record surface as a renewed local-TCB trust decision.
- Contextual Codex bundle metadata at `/Users/bill/.cache/codex-runtimes/codex-primary-runtime/runtime.json` must remain SHA-256 `f3a49c21eb32230654c0b6318a22311f48c8bdd857b60ede08ca5dc90d9c1e17` with exact fields `artifactToolVersion="2.8.48"`, `bundleFormatVersion=2`, `bundleVersion="26.819.11345"`, `pythonVersion="3.12.13"`, `targetArch="arm64"`, and `targetPlatform="darwin"`. This is context only, not publisher provenance or a file-level signed manifest.
- The selected surface contains exactly seven Mach-O files: `bin/python3.12`; `lib/libtcl9.0.dylib`; `lib/libpython3.12.dylib`; `lib/libtcl9tk9.0.dylib`; and `lib/python3.12/lib-dynload/{_tkinter,_dbm,_crypt}.cpython-312-darwin.so`. Require arm64 file identity, current ad-hoc/linker signature verification, and dependencies/RPATHs that remain Apple-system or TCB-contained for all seven. `spctl` rejection is expected for this ad-hoc local TCB and is disclosed, not treated as publisher trust.
- The only approved bootstrap package is `lib/python3.12/ensurepip/_bundled/pip-25.0.1-py3-none-any.whl`, SHA-256 `c46efd13b6aa8279f33f2864459c8ce587ea6a1a59ee20de055868d8f7688f7f`. Base `site-packages`, base pip 26.2.1, and its executable `.pth` are excluded and may not run.
- Any path, ownership, version, platform, file-set, TCB digest, interpreter digest, ensurepip digest, or dynamic-library containment mismatch blocks the task; it is not permission to weaken the assumption.

## Expected behavior

- A fresh current-user-owned mode-0700 `/Users/bill/Library/Caches/AgriScope/WEB-PYTHON-ENV-001/agriscope-py312.*` root, distinct from every prior root, contains a copied-interpreter venv, private resolver/evidence paths, reviewed wheelhouse, immutable receipt, and a separate writable WEB-SEC run directory. This durable user-cache location is selected so an active reviewed environment survives multi-day goal continuations; it is still disposable and must be removed by the final cleanup contract.
- The venv is created with the exact base interpreter using `-I -B -S -m venv --copies --without-pip`; private pip 25.0.1 is then bootstrapped in-process only from the hash-verified bundled pip wheel using the copied private interpreter with `-I -B` and normal venv prefix processing. Ordinary `ensurepip` is not invoked because its child command would not preserve the bytecode flag. Base site-packages are never on `sys.path`.
- The exact 12 WEB-SEC product/test profile roots named below are parsed and verified against exact pins in `pyproject.toml` without shell evaluation. No validation-only root is allowed. Resolution is captured before installation; every selected artifact is an exact binary wheel from official PyPI HTTPS with a SHA-256 digest.
- Exact report URLs are downloaded with redirect/final-host checks, wheel bytes and internals are verified, and installation uses an explicit hash-bound local manifest with `--no-index --no-deps --require-hashes`.
- The environment passes version, isolation, dependency-closure, native-import, `pip check`, collection, inventory, and repository-hygiene checks. It remains `IMPLEMENTED` while retained for WEB-SEC and reaches `VERIFIED` only after the exact approved cleanup.
- This is not a general AgriScope Python environment. It is invalid for raster processing, NumPy-based computation, ASGI serving, migrations, Redis/Dramatiq workers, JOSE, Ruff, mypy, or any later task that needs an excluded root. Such use requires a new contract and separately accepted environment.

## Scope and ownership

- Orchestrator only: `.agents/tasks/WEB-PYTHON-ENV-001-task-brief.md`.
- Provisioning owner owns validation and literal rejection cleanup of exactly `/Users/bill/Library/Caches/AgriScope/WEB-PYTHON-ENV-001/agriscope-py312.ng9ITm`, creation/validation of the external rejection evidence beneath the task parent, and—only after that rejected leaf is proven absent—creation/validation of exactly one new generated leaf beneath that parent; it owns no repository path. Treat existing `/Users/bill/Library/Caches` as the trust anchor and require it to be a canonical current-user-owned mode-0700 non-symlink directory. With umask 077, create a missing `AgriScope` component using absolute `/bin/mkdir` without `-p`, immediately lstat/realpath/uid/mode-check it, then create/check the task parent the same way. If either exists, validate it rather than replacing it and fail closed on any symlink, non-directory, foreign owner, unexpected canonical path, or mode other than 0700. After the exact rejected-leaf cleanup, require no existing child matching `agriscope-py312.*`; create exactly one unpredictable fresh leaf with mode 0700 and validate its single basename component. Never use `HOME`, `~`, a glob-expanded target, or an unresolved variable.
- Repository `.venv`, manifests, source, tests, lockfiles, caches, node_modules, `.next`, and unrelated dirty/untracked paths are read-only.
- No other repository writer may run during the provisioning/discovery hygiene window.
- Prerequisite: WEB-SEC phase A's exact two-file JSON conversion and native/security review are complete and frozen at the two exact repository hashes recorded above. The original tracked YAML remains recoverable from Git for read-only semantic comparison; the provisioning owner receives no repository write ownership.
- Every prior `/private/tmp` environment is absent. The durable rejected leaf is evidence/cleanup scope only and is never an input to provisioning. Fresh provisioning starts only after that exact leaf is absent and uses only the accepted host TCB, current pinned `pyproject.toml` roots, and fresh network resolution/downloads; it consumes no prior environment venv, package, wheel, report, script, resolver, or dependency bytes.

## Bootstrap and isolation contract

- After this contract approval and immediately before the first Python invocation, independently generate and seal a second 943-record manifest with the exact algorithm and require it to equal the approved digest. Then revalidate current-user ownership, regular-file/non-symlink status, exact realpaths/modes, the exact deterministic 943-record TCB manifest/file set/digest, runtime metadata, interpreter/ensurepip digests, all seven Mach-O identities/linkages, Python 3.12.13, arm64/macOS platform, and ensurepip 25.0.1 immediately before and after every provisioner Python invocation and every later WEB-SEC Python validation/test batch. Generate manifests outside the TCB with absolute system `/usr/bin/ruby` using only its JSON/Digest stdlib under the sanitized environment. Record both digests per batch; any drift invalidates the environment and results.
- Invoke the base interpreter only as the absolute path above with `-I -B -S`; use it only to verify the approved TCB and create the copied venv. The copied private interpreter exclusively owns bootstrap and later package/test commands. Do not invoke base pip or import base site-packages.
- Create the venv with `--copies --without-pip`; prove the copied interpreter is a regular file beneath the private root, `sys.prefix` is the private venv, `sys.base_prefix` is the accepted TCB root, `site.ENABLE_USER_SITE` is false, and `sys.path` contains no base `site-packages`, repository `.venv`, user site, cwd injection, or outside package directory.
- Hash-check the bundled pip wheel, then invoke the copied private interpreter with `-I -B` and without `-S`. Before adding the wheel, assert `sys.prefix == <literal-private-venv>`, `sys.base_prefix == <accepted-TCB-root>`, `site.ENABLE_USER_SITE is false`, and `sys.path` contains the private empty site-packages but no base site-packages, repository `.venv`, user site, cwd injection, or other external package directory. Only then prepend the exact verified wheel and invoke `pip._internal.cli.main` in the same process with global `--isolated --disable-pip-version-check --no-input` followed by `install --ignore-installed --no-compile --no-index --no-deps --no-cache-dir <exact-wheel-path>`; require return code exactly zero. End that process so the bootstrap wheel is no longer on `sys.path`, then use a fresh copied-private-interpreter invocation `-I -B -m pip --version` plus `importlib.metadata` to prove exactly `pip==25.0.1` resolves from beneath the private venv and no pip distribution resolves from the TCB wheel or base site-packages. This installs only pip 25.0.1 into the venv without ordinary ensurepip or a child Python process. Record pip's RECORD and installed files. Resolver/installed inventory equality excludes exactly this bootstrap `pip==25.0.1`; no pip update or setuptools bootstrap is allowed.
- Every private pip invocation uses absolute `<private-venv>/bin/python -I -B -m pip --isolated` from a cwd beneath the private root with `--no-input --disable-pip-version-check --no-cache-dir`. Every other base/private/import/pytest/consumer Python invocation likewise includes CLI `-B`; bytecode suppression never relies on a `PYTHON*` environment variable ignored by `-I`.
- Use a sanitized allowlisted environment without repurposing `HOME`: clear/reject `PYTHONPATH`, `PYTHONHOME`, `RUBYOPT`, `RUBYLIB`, `GEM_HOME`, `GEM_PATH`, all alternate/extra index and find-links variables, trusted-host/cert/client-cert/keyring variables, HTTP(S)/ALL proxy variables, `NO_PROXY`, Requests/curl CA or netrc overrides, every `DYLD_*` and `LD_*`/preload/library-path variable, and `SSLKEYLOGFILE`; set a minimal system-only `PATH`, `NETRC=/dev/null`, `PIP_CONFIG_FILE=/dev/null`, `PIP_KEYRING_PROVIDER=disabled`, and `PYTHONNOUSERSITE=1`. Keep `TMPDIR`, `XDG_CACHE_HOME`, `PIP_CACHE_DIR`, reports, configs, logs, and any non-isolated tool cache beneath the private root. `-B` is the authoritative bytecode control.
- Revalidate private root/venv/interpreter realpath containment, owner, mode, and non-symlink status immediately before each resolve/download/install/inspect/test phase.

## Requirements and resolution contract

- Parse `[project].dependencies` and `[project.optional-dependencies].dev` with stdlib `tomllib` into data only; never source, interpolate, or shell-evaluate manifest text.
- The WEB-SEC pyproject-backed product/test root allowlist is exactly:
  - `fastapi==0.116.1`
  - `pydantic==2.11.7`
  - `SQLAlchemy==2.0.42`
  - `greenlet==3.2.4`
  - `GeoAlchemy2==0.17.1`
  - `psycopg[binary]==3.2.9`
  - `httpx==0.28.1`
  - `argon2-cffi==25.1.0`
  - `shapely==2.1.1`
  - `numpy==2.3.2`
  - `pytest==8.4.1`
  - `pytest-asyncio==1.1.0`
- No additional validation-only root is permitted. In particular, `PyYAML`, `yaml`, or any replacement YAML parser must be absent from resolver input/output, wheelhouse, staged extraction, installed metadata, import resolution, and receipt inventory.
- Each pyproject-backed root must occur byte-for-byte as one exact direct value in the appropriate `pyproject.toml` dependency array. Reject a missing, changed, duplicated, URL, VCS, local-path, wildcard, range, alternate-index, or marker-controlled root. Build resolver argv from exactly the 12 reviewed pyproject-backed roots in the listed order; do not pass the full project or `dev` extra to pip.
- Explicitly reject every other direct project/dev root from the resolver argv, including `uvicorn[standard]`, `pydantic-settings`, `alembic`, `redis`, `dramatiq`, `python-jose[cryptography]`, `rasterio`, `ruff`, and `mypy`. Their absence is intentional profile minimization, not permission to remove or change them in `pyproject.toml`.
- Resolve with private pip 25.0.1 using `--dry-run --ignore-installed --only-binary=:all: --index-url https://pypi.org/simple --report <private-report>` and the argv roots.
- Reject any report entry that is not an HTTPS `.whl` from `files.pythonhosted.org`, lacks SHA-256, contains `direct_url`/VCS/local input, or has mismatched exact metadata.
- Dependency equality uses PEP 503 canonical names and a PEP 508 fixed-point traversal of each report distribution's `Requires-Dist`. Start only from the 12 exact pyproject-backed roots with requested extras; evaluate markers with the exact private Python 3.12/macOS-arm64 environment and propagate dependency extras until stable. Every profile root pin must match, every selected report project must be reachable, every active dependency must be present and specifier-compatible, and the report set must equal the active profile closure. The required result is independently derived as exactly 30 resolved projects/wheels and 31 installed distributions including bootstrap `pip==25.0.1`; no deleted temporary report or bundle is an acceptance oracle or resolver/download/install input. Any count, root pin, active edge, project/version, wheel/hash/URL, installed inventory, or YAML-absence mismatch returns to contract review. A distribution reachable only from an excluded project root is forbidden.

## Exact artifact download

- Download each exact `download_info.url` captured in the immutable report, not a fresh name/version resolution. Invoke absolute `/usr/bin/curl` with `-q/--disable` as its first option so no curl config is loaded, no proxy/netrc/credential/header forwarding, HTTPS-only protocol, TLS verification, no redirect following, and an exact HTTP 200/effective-URL equality requirement on the original `files.pythonhosted.org` URL. Enforce a streaming hard output cap of 1 GiB independent of `Content-Length`; abort/remove a partial file on overflow, timeout, non-200, URL change, or host mismatch.
- Save using an orchestrator-generated canonical filename beneath the wheelhouse; maintain an explicit ordered JSON inventory containing canonical name, version, exact source URL, local realpath, size, and expected SHA-256.
- Recompute SHA-256 after download, immediately before install, and immediately after install; all must match the report. Reject extra/missing/duplicate normalized projects or files.

## Wheel-internal validation

- Parse wheel filename name/version/build/tags and require exactly one matching canonical `.dist-info` tree. `METADATA` Name/Version must match filename and report; `WHEEL` version/tags must be valid and intersect the private interpreter's supported tags.
- Allow only regular-file/directory ZIP members. Reject symlink/device/FIFO/socket/special members; NUL, backslash, drive, absolute, empty, dot/traversal, malformed Unicode, duplicate, Unicode-normalized, or case-fold-colliding paths.
- Reject every `.pth` member, every unexpected archive type, more than 100,000 entries, more than 2 GiB total uncompressed bytes, a member over 1 GiB, or compression ratio over 200:1 for members over 1 MiB.
- Reject an installable top-level `sitecustomize.py` or `usercustomize.py`; after installation recheck every active site directory before any subsequent Python command.
- Parse `RECORD`; require one row for every regular-file member (directory entries are excluded), no escaping destination, matching size, and valid SHA-256 for every file except exact RECORD/signature exceptions allowed by the wheel spec. Recompute and compare each recorded hash/size.
- Inspect every Mach-O executable, extension, and dylib in the archive's staged safe extraction and again after installation with absolute system tooling. Allow only Apple system dependencies under `/usr/lib` or `/System/Library`, or `@loader_path`/`@executable_path`/`@rpath` dependencies and LC_RPATH values whose complete canonical resolution remains beneath the private venv. Reject unresolved loader paths, absolute user/Homebrew/Library paths, external rpaths, or any dependency escape.
- Before installation, prove `purelib`, `platlib`, `scripts`, `data`, and include installation destinations resolve beneath the private venv. Install an explicit immutable local-URL requirements file—never a glob—with one exact project/wheel URL and `--hash=sha256:<report-hash>` per line using `--no-index --no-deps --require-hashes`.
- After installation, prove all installed scheme destinations and distribution files remain beneath the venv, no `.pth` exists, and bootstrap pip's recorded files are unchanged.

## Validation and repository hygiene

- Post-install inventory must equal the active WEB-SEC profile report closure plus exactly `pip==25.0.1`; `pip check` must pass. No excluded direct root may appear unless it is independently required by and reachable from the active profile closure; if that happens, stop for contract review rather than silently accepting it.
- Native/direct imports run under `<private-python> -I -B` and must succeed: `fastapi`, `pydantic`, `sqlalchemy`, `greenlet`, `geoalchemy2`, `psycopg`, `httpx`, `argon2`, `shapely`, `numpy`, `pytest`, and `pytest_asyncio`. The import gate must prove excluded top-level modules `yaml`, `uvicorn`, `pydantic_settings`, `alembic`, `redis`, `dramatiq`, `jose`, `rasterio`, `ruff`, and `mypy` do not resolve from the venv or TCB.
- Walk every governed repository `.py` file with stdlib `ast` and record static/literal dynamic third-party imports for the profile proof. Any non-literal dynamic third-party import, unresolved required import, or import outside the exact active 12-root closure returns to contract review. No repository-wide reflection sandbox is part of this profile because no validation-only parser is installed or importable.
- AgriScope smoke imports explicitly insert the literal repository root read-only under `-I` and import `apps.api.agriscope_api.application`, the auth/farm/organization/satellite routes, core config/logging/security, and satellite service.
- Test discovery uses a hash-recorded launcher created beneath the private WEB-SEC run directory and the exact private interpreter with `-I -B`. The launcher inserts only the literal repository root into `sys.path`, changes cwd to that literal root because contract fixtures use repository-relative paths, imports the private `pytest`, and calls `pytest.main` with absolute target paths. It runs with `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`, explicit `-p pytest_asyncio.plugin`, `-p no:cacheprovider`, private `TMPDIR`, `XDG_CACHE_HOME`, and `--basetemp`; it collects `tests/unit`, `tests/contract`, and `tests/integration/test_foundation_api.py` without running tests or enabling coverage/cache plugins. The same launcher boundary is used for handed-off WEB-SEC tests. Post-run scans prove no repository pycache/cache artifact.
- Before provisioning, serialize all repository writers and record an exact manifest of repository regular files/symlinks excluding `.git`, node_modules, ignored `.next`, and the private temp root; record a separate exact manifest for repository `.venv` and the set of ignored Python artifacts (`__pycache__`, `.pytest_cache`, `.mypy_cache`, `.coverage*`). Recompute after discovery and require byte/path/mode equality and no new ignored artifact. Git status/diff is supporting evidence, not the attribution mechanism.
- Create an immutable JSON profile-proof artifact at provisioning and recompute a final proof after the WEB-SEC implementation is frozen, immediately before its full consumer validation/review. Its source/test file set is every regular `.py` file under `apps/api/agriscope_api`, every regular `.py` file under `packages/geospatial/agriscope_geospatial`, every regular `.py` file under `tests/unit` and `tests/contract`, plus exact `tests/integration/test_foundation_api.py`; record normalized relative path, mode, size, SHA-256, and deterministic aggregate digest. Parse every file with stdlib `ast`, record all static imports including imports nested in functions/try blocks, and record literal targets passed to `importlib.import_module` or `__import__`; any non-literal dynamic third-party import blocks for review.
- The profile proof maps every included root to either exact importing file(s), the private test launcher, or an exact active `Requires-Dist` edge needed by such a consumer. At minimum it must prove: FastAPI/Pydantic route and TestClient consumers; SQLAlchemy/greenlet database consumers; GeoAlchemy2 model consumers; psycopg driver/integration consumers; httpx TestClient/provider consumers; argon2 auth consumers; Shapely/NumPy geometry consumers; and pytest/pytest-asyncio launcher/tests. No planned-consumer exception is allowed. It separately lists every excluded root/module, including `yaml`, and proves neither the resolved graph nor final source import graph requires one.
- Any file-set/hash/import change after the final proof, unresolved required import, excluded module/root, new third-party top-level import without a mapped active distribution, or profile root lacking a mapped consumer/required closure edge returns this environment and WEB-SEC to contract review. No package mutation, host fallback, base-site import, or ad hoc parser is allowed. The final full unit/contract/integration validation must run against the exact final proof digest.

## Receipt and controlled handoff

- Create an immutable private receipt containing: literal accepted root/venv/interpreter realpaths and the separately recorded rejected-root identities; owner/modes; Python/pip/platform/ABI; accepted TCB statement, exact manifest algorithm/file count, and provisioner-phase before/after TCB digests; ensurepip-wheel hash and bootstrap-pip RECORD; sanitized environment/loader controls and exact commands; resolver-report hash; exact 12-root PEP 508 closure; ordered wheel URL/name/version/size/hash inventory; explicit YAML-parser absence evidence; wheel-internal and Mach-O linkage checks; installed inventory; `pip check`; imports/collection; initial profile-proof file/root-consumer maps; and repository/`.venv` hygiene manifests/results. The final consumer handoff adds the separately immutable final profile-proof digest, consumer-batch TCB receipts, and validation results beneath the runs directory without mutating the sealed provisioning receipt.
- Seal receipt, report, wheelhouse, and venv against mutation after acceptance. Create a distinct current-user-only writable `runs/WEB-SEC-CONTRACT-001` directory for test temp/cache/output.
- Transfer exclusive consumer authority to the WEB-SEC implementation owner: read/execute the venv and write only beneath that run directory. The consumer must preserve the same absolute-interpreter `-I -B`, loader/environment clearing, before/after TCB manifest, no cacheprovider/plugin-autoload, private temp/cache, no-package-command boundary. No install/uninstall/pip mutation is permitted.
- Every WEB-SEC consumer Python discovery/test/import batch creates a new immutable, monotonically named receipt beneath `runs/WEB-SEC-CONTRACT-001` containing the exact argv and cwd; allowlisted non-secret environment controls with sensitive values redacted; accepted-root/interpreter/profile-proof identities; TCB before/after record count and digest; repository/`.venv`/ignored-artifact hygiene before/after digests; stdout/stderr SHA-256; exit status; collected/executed test identities and counts; and result. A batch receipt is sealed after the batch and never appended or rewritten. Any missing, mutable, mismatched, or failed receipt invalidates that batch.
- After provisioning acceptance this task is `IMPLEMENTED`, not `VERIFIED`, while the directory is retained. After WEB-SEC implementation/reviews no longer need it, the orchestrator must explicitly authorize cleanup before either task is marked `VERIFIED`.

## Cleanup contract

- The historical `/private/tmp/agriscope-py312.xQbk8t`, `/private/tmp/agriscope-py312.QCh9MY`, `/private/tmp/agriscope-py312.1grxdH`, and `/private/tmp/agriscope-openapi-json.4IOLAA` paths are already absent. They transferred no consumer authority and are not future cleanup targets or live evidence sources. Do not recreate, reuse, or claim new evidence for those literal paths.
- Before fresh provisioning, clean only the exact rejected literal leaf `/Users/bill/Library/Caches/AgriScope/WEB-PYTHON-ENV-001/agriscope-py312.ng9ITm`. First serialize task-parent writers; validate the task parent and leaf as current-user-owned mode-0700 canonical non-symlink directories; require the leaf basename to equal exactly `agriscope-py312.ng9ITm`; require zero process/open-file use and authority `WITHHELD`/never transferred; prove no venv exists, no Python/pip/network/package/consumer-test/repository-write action occurred, and any resolver/wheelhouse/staging/work directories are absent or empty.
- Before deleting that rejected leaf, create one separately validated current-user mode-0700 non-symlink evidence directory beneath the task parent whose basename does not match `agriscope-py312.*`. Preserve there only immutable non-executable JSON/text/hash evidence: the exact `TCB-DRIFT-receipt.json` SHA-256 `9528012e7adbbed427c62017680955c9a06a4ecac0ed9306234ebec34501534a`, its self-hash companion SHA-256 `58cc2595db36b089912b5c740905421324a06875e96d62abb529e4bdd3e9d534`, a complete deterministic rejected-leaf inventory, current and superseded TCB identities, runtime metadata identity, repository-hygiene equality, zero-use/zero-authority facts, rejection reason, and exact cleanup authorization. Do not copy or retain an executable script, interpreter, venv, wheel, package, resolver/install input, or other executable/dependency byte.
- Independently rehash that preserved rejection evidence, then remove only the validated rejected literal leaf with a symlink-resistant literal removal while holding the task-parent/no-concurrent-writer boundary. Confirm its absence and seal a cleanup receipt in the external evidence directory. Never use a glob, unresolved variable, combined/broad cache path, `HOME`, `~`, repository path, parent cache directory, or sibling task directory. Only after this receipt passes may the provisioning owner create a distinct fresh leaf.
- After WEB-SEC use, the only later cleanup target is the new accepted durable-cache leaf recorded in its canonical receipt. No rejected leaf may be recreated or treated as an accepted-root identity.
- Immediately before deletion, serialize all task-parent writers and verify the exact literal leaf is current-user owned, mode 0700, and a non-symlink directory. Require `dirname(realpath(leaf))` to equal exactly `realpath(/Users/bill/Library/Caches/AgriScope/WEB-PYTHON-ENV-001)`, and require its basename to be one non-empty component matching `agriscope-py312.` plus the recorded generated suffix with no slash, dot component, or traversal. Revalidate the private task parent, receipt/root identity, immutable receipt/venv/wheelhouse/resolver/staging hashes, every immutable consumer batch receipt, final source proof, repository hygiene, and zero process/open-file use.
- Remove only that validated literal leaf with a symlink-resistant literal removal while holding the validated private-parent/no-concurrent-writer boundary; confirm absence and write a separate cleanup receipt beneath the retained task parent. The cleanup receipt enumerates every sealed consumer batch receipt and final manifest hash before deletion. Retain the task parent and cleanup receipt; do not remove the task parent or `/Users/bill/Library/Caches/AgriScope`.
- Final `WEB-PYTHON-ENV-001` and WEB-SEC `VERIFIED` status requires the accepted durable leaf to be absent, the cleanup receipt preserved outside that leaf, and final repository hygiene unchanged.

## Risks

- The Codex-bundled interpreter is an explicitly accepted local TCB, not upstream publisher provenance. A signed, committed Python toolchain/lock remains later governance work.
- The repository lacks a Python lockfile, so dependencies are per-run report/hash bound rather than cross-machine reproducible.
- Verified wheels still contain third-party/native code that executes on import; this task constrains acquisition/install behavior but does not declare the graph vulnerability-free.
- Deterministic JSON at a `.yaml` path is intentionally valid YAML 1.2/OpenAPI and preserves existing references, but extension-assuming external tools remain a disclosed compatibility risk; phase-A tests and review fail closed before fresh provisioning.
- User cache locations may persist longer than `/private/tmp`; this is intentional for active multi-day review but increases the importance of exact literal cleanup before verification.
